#---
#server:
#  port: ${TURBINE_SERVICE_PORT:9999}
#
#turbine:
#  amqp:
#    port: ${TURBINE_AMQP_PORT:8989}
#  aggregator:
#    clusterConfig: microservices-cluster
#  appConfig: eureka-server,frontent-server
#  clusterNameExpression: "'microservices-cluster'"
#
#---
#spring:
#  profiles: prod
#
#---
#spring:
#  profiles: test

server:
  port: 7979

management:
  port: 7980
  security:
    enabled: false
    endpoints:
      web:
        exposure:
          include: hystrix.stream
        base-path: /